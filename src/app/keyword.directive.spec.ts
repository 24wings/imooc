import { KeywordDirective } from './keyword.directive';

describe('KeywordDirective', () => {
  it('should create an instance', () => {
    const directive = new KeywordDirective();
    expect(directive).toBeTruthy();
  });
});
